# .bashrc

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

export EDITOR=nvim
export PATH=$HOME/go/bin:$PATH
export XDG_RUNTIME_DIR=/home/d/.swaytmp
